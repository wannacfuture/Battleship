name: PR Reviewer Changed

on:
  pull_request:
    types:
      - review_requested

jobs:
  add_to_project:
    runs-on: windows-latest

    steps:
      - run: echo ${{ github.event.pull_request.requested_reviewers[github.event.pull_request.requested_reviewers.length - 1].login }}
